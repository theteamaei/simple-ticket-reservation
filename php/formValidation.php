<?php
    require_once "connect.php";

    // $data = $_POST;

    // if (empty($data['first_name']) || empty($data['last_name']) || empty($data['email']) || empty($data['password']) || empty($data['confirm-password'])) {
    //     echo("Please fill out the form completely!");
    // }

    // if ($data['password'] !== $data['confirm-password']) {
    //     die("Password Doesn't Match!");
    // }
    
    

    
    
    
?>